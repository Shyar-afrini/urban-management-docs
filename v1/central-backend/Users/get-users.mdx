---
title: "Get Users"
description: "Endpoint to retrieve an array of users"
---

<Info>Authenticated route</Info>
## GET /users

<ResponseField name="users" type="User Query Object">
  <Expandable title="Filters">
    <ResponseField name="name" type="string">
      User's fullname
    </ResponseField>

    <ResponseField name="age" type="integer" />

    <ResponseField name="nationality" type="string" />

    <ResponseField name="phoneNumber" type="string">
      could be primary or secondary
    </ResponseField>

    <ResponseField name="gender" type="enum" example="MALE">
      Male or Female
    </ResponseField>

    <ResponseField name="active" type="boolean">
      True or False
    </ResponseField>

    <ResponseField name="order" type="string" example="asc">
      ascending or descending, use ("asc" or "desc")
    </ResponseField>

    <ResponseField name="documentNumber" type="string">
      Provided document number
    </ResponseField>

    <ResponseField name="maritalStatus" type="enum" example="MARRIED">
        Could be Married, Single, or Divorced
    </ResponseField>

  </Expandable>
</ResponseField>

---

### Helpers

Type and schema helpers for parsing and inferring types

```ts ServerUser
//? Use this for type infer
const userData: ServerUser;
```

```ts selectUserSchema
function parse(
  schema: ZodSchema<selectUserSchema>,
  data: unknown
): TResult<ServerUser> {
  const result = schema.safeParse(data);
  if (!result.success) {
    return {
      success: false,
      error: {
        code: BAD_REQUEST_STATUS,
        message: BAD_REQUEST_ERR,
        details: result.error.flatten(),
      },
    };
  }
  return { success: true, data: result.data };
}
```

<ResponseExample>
```json Success
{
    "success": true,
    "data": [
        {
            "id": "7e11fb1c-4a36-4b35-a168-89cf9f6ab8c1",
            "fullname": "Shyar Afrini",
            "phoneNumber": "07518202481",
            "secondPhoneNumber": "07518202485",
            "profilePhoto": "http://link-to-profile-photo",
            "nationality": "Syrian",
            "maritalStatus": "SINGLE",
            "gender": "MALE",
            "age": 22,
            "documentNumber": "onasof8297gf8g827e1r1f1f3f",
            "isActive": true,
            "lastLoginAt": null,
            "createdAt": "2025-09-11T13:38:20.424Z",
            "updatedAt": "2025-09-11T13:38:20.424Z"
        },
        // etc...
    ]
}
```
</ResponseExample>
