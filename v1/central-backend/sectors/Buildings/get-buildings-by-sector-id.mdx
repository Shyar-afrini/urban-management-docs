---
title: "Get Building By ID"
description: "Get Buildings By ID Endpoint"
---

<Info>Authenticated route</Info>
## GET /sectors/\<uuid\>/buildings

### Helpers

Type and schema helpers for parsing and inferring types

```ts Building
//? Use this for type infer
const buildingData: Building[];
```

```ts selectBuildingSchema
function parse(
  schema: ZodSchema<selectBuildingSchema>,
  data: unknown
): TResult<Building[]> {
  const result = schema.safeParse(data);
  if (!result.success) {
    return {
      success: false,
      error: {
        code: BAD_REQUEST_STATUS,
        message: BAD_REQUEST_ERR,
        details: result.error.flatten(),
      },
    };
  }
  return { success: true, data: result.data };
}
```

<ResponseExample>
```json Success
{
    "success": true,
    "data": [
        {
            "id": "170b6733-f0d2-49bb-a9e6-c9ee016022ad",
            "sectorId": "08273d45-2d1f-44ed-873e-31f657b100a6",
            "name": "Cihan A Building",
            "image": "http://link-to-this-image",
            "addressId": "66a22c16-b942-4d42-84c6-c10c10ebfdb9",
            "floorCount": 12,
            "yearBuilt": 2025,
            "status": "ACTIVE"
        },
        //? etc
    ]
}
```
</ResponseExample>
