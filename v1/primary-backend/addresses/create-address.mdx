---
title: "Create Address"
description: "Create new address"
---

<Info>Authenticated route</Info>
## POST /sectors/address

<Expandable title="Address Schema">
  <ResponseField
    name="street"
    type="string"
    example="Main Street"
    required
  />
  <ResponseField
    name="city"
    type="string"
    example="New York"
    required
  />
  <ResponseField
    name="state"
    type="string"
    example="NY"
    required
  />
  <ResponseField
    name="zipCode"
    type="string"
    example="10001"
    required
  />
  <ResponseField
    name="country"
    type="string"
    example="USA"
    required
  />
</Expandable>

### Helpers

Type and schema helpers for parsing and inferring types

```ts Address
//? Use this for type infer
const newAddress: NewAddress;
```

```ts InsertAddressSchema
function parse(
  schema: ZodSchema<InsertAddressSchema>,
  data: unknown
): TResult<NewAddress> {
  const result = schema.safeParse(data);
  if (!result.success) {
    return {
      success: false,
      error: {
        code: BAD_REQUEST_STATUS,
        message: BAD_REQUEST_ERR,
        details: result.error.flatten(),
      },
    };
  }
  return { success: true, data: result.data };
}
```

<ResponseExample>
```json Success
{
    "success": true,
    "data": {
        "id": "7e11fb1c-4a36-4b35-a168-89cf9f6ab8c1",
        "street": "Main Street",
        "city": "New York",
        "state": "NY",
        "zipCode": "10001",
        "country": "USA",
        "createdAt": "2025-09-11T13:38:20.424Z",
        "updatedAt": "2025-09-11T13:38:20.424Z"
    }
}
```
</ResponseExample>
